# 仓位模块设计：看板与预警

本文是 `ENERGREX_参数量化_约束_回测_可信度设计.md` 的落地评估与设计。
原文的四层结构（收益层 / 风险层 / 仓位层 / 验证层）方向是对的，但按
1→2→3→4 的顺序建会卡死：**决定前三层值不值得建的，恰恰是第四层，而第四层
需要 8 个月以上才能出结论。**

本文给出的解法是按「是否依赖预测力」重新切分，而不是按原文的层次切分。

---

## 一、可行性评估

### 结论速查

| 能力 | 是否依赖预测力 | 数据是否就绪 | 结论 |
|---|---|---|---|
| 硬约束现状看板 | 否 | 是（`positions` 表） | **立即可建** |
| 突破硬约束告警 | 否 | 是 | **立即可建** |
| 波动率 / 最大回撤 | 否 | 是（yfinance 价格史） | **可建，需新增计算** |
| 组合贝塔 | 否 | 是 | **可建，需新增计算** |
| 相关性 / 风险贡献 | 否 | 是 | **可建，需新增协方差** |
| 评分驱动的仓位系数 | **是** | **否** | **被验证阻塞 → 影子模式** |

关键判断：原文六项硬约束和大部分风险指标**都不需要预测力**。它们回答的是
「万一我错了，会不会死」，不是「我对的时候能赚多少」。哪怕所有评分都是纯噪
声，这些约束照样成立、照样保护你。

### 已具备的输入

- **真实持仓**：`account/repository.py::load_positions(acct_id)` 读 SQLite
  `positions` 表，Firstrade 同步而来，含 `market_value`。硬约束层的全部输入。
- **产业链分组**：`scoring/scoring_engine.py::TICKER_CATEGORY`，五类
  （AI芯片 / AI软件SaaS / 网络安全 / 半导体设备 / 大型科技）。
- **价格历史**：yfinance 按需拉取，`kelly_backtest.py` 已在用。
- **告警约定**：`account/risk.py` 已确立 `trigger(...) -> dict | None` 的写
  法（`delta_drift_trigger` / `vix_spike_trigger`）。新告警沿用，不另起炉灶。

### 缺口

- **相关性矩阵、组合贝塔、风险贡献：代码里一行都没有。** 全新建设。
- **`TICKER_CATEGORY` 只覆盖 87 只跟踪股票**。实际持仓若含表外标的，分组会
  落空——必须显式处理「未分类」而不是静默归零（否则产业链集中度会被低估）。

### 被阻塞的部分：为什么评分暂时不能驱动仓位

两条独立的证据：

**1. 快照历史远远不够。** `data/score_snapshots.csv` 目前 4 个日期
（2026-07-11、07-14、07-30、07-31），跨度 20 天。而 `PRICE_BOUNDARY_STANDARD.md`
的验证门槛要求 ≥252 个日历日、≥100 个时点快照、切分后 ≥30 个样本外观测。按每
交易日一次，252 天要到 2027 年 4 月左右。原文「牛熊市均有效」更严苛——现有快
照全落在同一段行情里。

**2. 综合评分目前没有可证明的区分度。**
`scoring/weight_config_backtest_report.json`：

| 方案 | 头尾分档收益差 |
|---|---|
| growth_only | +0.117 |
| expectation_gap_only | +0.089 |
| **current_final** | **−0.020** |
| equal_weighted | −0.055 |
| valuation_only | −0.132 |

综合评分的高分组**跑输**低分组。该报告自身已标注这是近似回测、存在生存者偏
差、不能证明预测力——但它同样**没有提供任何支持预测力的证据**。

仓位公式的本质是「用这个分数放大敞口」。分数没有边际信息时，公式做的是**给噪
声加杠杆**。所以第一版：硬约束决定仓位，评分只进影子记录。

---

## 二、理论科普

原文列了一堆指标名，这节解释每个到底在防什么。

### 为什么硬约束不需要预测力

预测类工具回答「买什么」，约束类工具回答「错的时候别死」。后者不需要知道未来。

举例：「单股不超过 15%」这条规则，在你选股全对时会让你少赚，在你选股全错时
会让你活下来。它的价值不来自预测准确度，来自**限制单次判断错误的破坏半径**。
这类规则今天就能建、今天就能验证——拿实际持仓一对照即可。

### 风险贡献 ≠ 仓位占比

最容易被忽略的一条。

一只波动率是组合平均两倍的股票，占 10% 仓位，可能贡献 25–30% 的组合风险。你
看着仓位表觉得「才 10%，很分散」，实际上组合的涨跌有近三分之一由它决定。

风险贡献衡量的是「这只股票占了组合总波动的多少」，而不是「占了多少钱」。原文
把「单股最高风险贡献」和「单股最高仓位」并列成两条独立约束，是对的——它们约
束的不是同一件事。

### 相关性：为什么六只 AI 芯片股不叫分散

分散的本质不是「持有多少只」，是「它们会不会一起跌」。

两只相关系数 0.9 的股票，各持 10%，风险上接近于一只持 19%（而非 20% 的一半）。
如果持仓集中在同一条产业链，名义上 6 只股票，实质可能是 1–2 个独立赌注。

这是原文「单产业链最高仓位」和「检查相关性」并存的原因：前者用行业标签近似，
后者用价格数据实测。标签会骗人（一只公司可能横跨两条链），相关性不会。

### 组合贝塔：你在赌市场还是赌选股

贝塔衡量组合相对大盘的放大倍数。组合贝塔 1.5 意味着大盘跌 10%，组合结构性地
跌 15%——这部分跟你选股水平无关，纯粹是市场敞口。

设贝塔上限的意义：把「我看好这几家公司」和「我在加杠杆做多整个市场」这两件事
分开。很多人以为自己在做前者，实际在做后者。

### 四个评价指标的区别（分母不同）

| 指标 | 分母 | 回答的问题 |
|---|---|---|
| 夏普比率 | 总波动 | 每承担一单位波动换来多少超额收益 |
| 索提诺比率 | **下行**波动 | 同上，但不惩罚上涨的波动 |
| 卡玛比率 | **最大回撤** | 每承受一单位「最痛时刻」换来多少收益 |
| 条件风险价值 CVaR | **尾部均值** | 最坏的那 5% 情况下，平均亏多少 |

夏普把上涨波动也当成风险，对高成长股不公平，所以有了索提诺。卡玛更贴近真实体
感——人是被最大回撤逼到割肉的，不是被标准差。CVaR 专门看尾部，回答「最坏情况
有多坏」，而不是「平均有多坏」。

### 为什么「每次只修正部分偏离」

原文动态流程第 9 步。两个原因：

1. **交易成本**：每次全额调仓，摩擦会吃掉大部分理论收益。原文「扣除交易成本
   仍有效」这条验证标准就是在防这个。
2. **避免追涨杀跌**：目标仓位由价格驱动，价格波动会让目标仓位跟着抖。全额跟随
   = 高位加仓、低位减仓。只修正一部分（例如偏离量的 1/3），相当于给系统加阻尼。

---

## 三、模块设计

### 数据流

```
真实持仓 (positions 表)  ──┐
产业链分组 (TICKER_CATEGORY)├──> 约束引擎 ──> 看板：现状 vs 上限
价格历史 (yfinance)       ──┘       │        预警：突破时告警
                                    │
评分快照 (score_snapshots) ─────────┴──> 影子建议（只记录，不驱动仓位）
```

### 看板：显示现状与上限的距离

每条硬约束一行，显示「当前值 / 上限 / 余量」：

| 约束 | 数据来源 | 依赖预测力 |
|---|---|---|
| 单股最高仓位 | `positions.market_value` 占比 | 否 |
| 单产业链最高仓位 | 同上 + `TICKER_CATEGORY` | 否 |
| 最低现金比例 | 账户现金 / 总资产 | 否 |
| 组合贝塔上限 | 各股贝塔按市值加权 | 否 |
| 单股最高风险贡献 | 协方差矩阵 | 否 |
| 单次最大调整比例 | 交易记录 vs 上一快照 | 否 |

**设计原则：看板只陈述事实，不给建议。** 「AI芯片占 42%，上限 35%，超限 7%」
是事实；「建议减仓 NVDA」是建议，属于影子层。

### 预警：突破时告警

沿用 `account/risk.py` 既有约定：

```python
def concentration_trigger(positions, limits) -> dict | None:
    """Return a trigger when any concentration limit is breached."""
```

返回 `None` 表示无事。这样新告警能直接接进现有的告警汇总逻辑。

告警分两级：
- **突破**：已超上限，需要处理
- **逼近**：达到上限的 90%，提前知会

### 影子层：给建议但不执行

这是攒验证数据的唯一途径——**不产生建议，就永远无法评估建议好不好**。

每个交易日记录一行：

| 字段 | 说明 |
|---|---|
| `date` | 快照日期 |
| `ticker` | 标的 |
| `suggested_action` | 系统建议（加/减/持有） |
| `suggested_weight` | 系统建议仓位 |
| `actual_weight` | 你的实际仓位 |
| `score_snapshot` | 当时各维度分数 |
| `price` | 当时价格 |

8 个月后这张表就是真实的样本外数据集：系统当时说什么、你当时做了什么、后来发
生了什么。**验证层需要的一切都在里面。**

在此之前，影子建议在界面上必须**与看板事实明确分区**，且标注未验证状态。

---

## 四、分期

| 阶段 | 内容 | 前置条件 |
|---|---|---|
| **P0** | 硬约束看板（单股/产业链/现金/集中度）+ 突破告警 | 无 |
| **P1** | 风险监控（波动率/最大回撤/贝塔） | P0 |
| **P2** | 相关性矩阵 + 风险贡献 | P1 |
| **P3** | 影子建议记录表 | P0（不必等 P1/P2） |
| **P4** | 验证：回测 + 六条可信度检验 | 快照 ≥252 天 |
| **P5** | 视 P4 结论决定评分是否驱动仓位 | P4 通过 |

P3 应尽早启动——它是纯记录，越早开始，P4 越早能做。

---

## 五、明确不做的事

1. **不做自动下单。** 与 ENERGREX 既有风控原则一致。
2. **第一版不让评分参与仓位计算。** 理由见第一节，直到 P4 给出结论。
3. **不引入新的评分维度。** 现有五维尚未验证，加维度只会让验证更难。
4. **不做「最优仓位」求解。** 均值-方差最优化对输入误差极度敏感；输入本身未
   经验证时，最优化只会把误差放大。第一版用等权 + 硬约束截断。

---

## 六、待定

- **各条硬约束的具体数值**（单股上限 15%？产业链上限 35%？现金下限 10%？）
  应由使用者按自身风险承受度设定，不由系统预设。
- **实际持仓中不在 `TICKER_CATEGORY` 内的标的如何分组**——需要人工补映射，或
  显式计入「未分类」并在看板上单列。
