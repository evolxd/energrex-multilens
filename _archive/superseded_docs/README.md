# 归档说明

2026-08-27 归档。`ARCHITECTURE_REVIEW.md`（2026-06-17写，2026-06-20后没再改过）
和 `PROJECT_STATUS.md`（标题写着"2026-06-13"，同样2026-06-20后没再改过）都是
静态 markdown 版本的架构概览/项目状态说明，跟 `docs/system_map.html` 是同一个
用途——但两个多月没更新，内容已经跟不上代码（比如还在说旧的页面结构）。

`docs/system_map.html` 取代了这两份的位置：可交互、按代码实测生成、带状态
过滤（核心在跑/搭好没启用/历史遗留/已解决），而且约定了往后怎么维护
（见该文件内的说明）。

没有归档 `HANDOFF.md`——那是不同性质的东西，按时间顺序记录具体改动的详细
叙述（含 commit 引用），跟 `system_map.html` 的"当前状态快照 + 精简变更记录"
不是一回事，两者可以并存。
